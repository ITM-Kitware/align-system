_target_: align_system.algorithms.ollama_inference_engine.OllamaInferenceEngine

# NOTE: must be a non-reasoning model; harmony-format models like
# gpt-oss return an empty response under ollama's schema-constrained
# generation (the grammar conflicts with their channel tokens)
model: gemma4:12b
temperature: 0.0
num_ctx: 8192
