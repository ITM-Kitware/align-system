# @package _global_
defaults:
  - override /adm: phase2_pipeline_zeroshot_comparative_regression
  - override /interface: ta3
  - override /adm_component/alignment@adm.step_definitions.scalar_alignment: random_effects_tuple
  - override /inference_engine@adm.structured_inference_engine: claude_haiku

interface:
  session_type: adept
  training_session: full
  username: "testrun-pipeline_zeroshot_comp_reg"
  domain: "p2triage"

driver:
  apply_action_filtering: false
force_determinism: true
align_to_target: true