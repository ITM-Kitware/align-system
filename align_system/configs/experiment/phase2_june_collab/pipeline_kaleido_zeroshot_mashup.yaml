# @package _global_
defaults:
  - override /adm: phase2_pipeline_kaleido_zeroshot_mashup
  - override /interface: ta3

interface:
  session_type: adept
  training_session: full
  username: "pipeline_june2025_kaleido_zeroshot_mashup"
  domain: "p2triage"

driver.apply_action_filtering: false

force_determinism: true
align_to_target: true
