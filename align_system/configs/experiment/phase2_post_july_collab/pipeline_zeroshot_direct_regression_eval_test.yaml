# @package _global_
defaults:
  - override /adm: phase2_pipeline_direct_regression
  - override /interface: ta3
  - override /adm_component/alignment@adm.step_definitions.scalar_alignment: medical_urgency_weighted_scalar

interface:
  api_endpoint: "https://darpaitm.caci.com"
  session_type: eval
  training_session: null
  username: "testrun-ALIGN-ADM-DirectRegression-Mistral-7B-Instruct-v0.3"
  domain: "p2triage"

adm:
  step_definitions:
    direct_regression:
      enable_caching: true

driver:
  apply_action_filtering: false

force_determinism: true
align_to_target: true
save_last_unstructured_state_per_scenario: true

hydra:
  run:
    dir: 'phase2_july_post_rq2_eval_live_test/direct_regression/${now:%Y-%m-%d__%H-%M-%S}'
