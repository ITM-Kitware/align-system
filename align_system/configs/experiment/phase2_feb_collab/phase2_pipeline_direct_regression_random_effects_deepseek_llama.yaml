# @package _global_
defaults:
  - override /adm: phase2_pipeline_direct_regression
  - override /interface: ta3
  - override /adm_component/alignment@adm.step_definitions.scalar_alignment: random_effects_tuple

interface:
  api_endpoint: "https://darpaitm.caci.com"
  session_type: adept
  training_session: full
  username: "testrun-pipeline_direct_deepseek_llama"
  domain: "p2triage"

adm:
  structured_inference_engine:
    model_name: deepseek-ai/DeepSeek-R1-Distill-Llama-8B

  step_definitions:
    direct_regression:
      enable_caching: true

driver:
  apply_action_filtering: false

force_determinism: true
align_to_target: true
