# @package _global_
defaults:
  - override /adm: phase2_pipeline_direct_regression
  - override /interface: ta3
  - override /adm_component/alignment@adm.step_definitions.scalar_alignment: random_effects_tuple

interface:
  api_endpoint: "https://darpaitm.caci.com"
  session_type: eval
  training_session: null
  username: "testrun-pipeline_direct"
  domain: "p2triage"

adm:
  step_definitions:
    direct_regression:
      enable_caching: true

driver:
  apply_action_filtering: false

force_determinism: true
align_to_target: true
save_last_unstructured_state_per_scenario: true

hydra:
  run:
    dir: 'phase2_feb2026_results_local/phase2_pipeline_direct_regression_live_eval_test/${now:%Y-%m-%d__%H-%M-%S}'
